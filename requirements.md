# Image Masking Tool

## 功能

- 使用者可以上傳圖片，一開始只要支援 png, bmp, jpg 即可，之後再支援更多圖片格式
- 使用者上傳圖片後，這個工具會透過 OCR 抓取出圖片中的位置和座標
- 使用者可以用滑鼠選擇圖片中的文字
- 使用者可以將所選擇的文字進行遮罩，會直接覆蓋一層黑色色塊在圖片上方，也可以將遮罩去除
- 使用者可以將有遮罩的圖片進行儲存

## 技術細節的想法

- 前端需要有 OCR 相關的 wasm
- 需要在圖片上疊加一層透明的圖層和文字，營造一種使用者是直接用滑鼠選擇文字的感覺
- 這是一個純前端的工具，不會呼叫外部 API 或後端